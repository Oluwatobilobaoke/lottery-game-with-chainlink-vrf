function printMyName(name) {
  const myName = name;
  const company = "Dyson Protocol";
  const compliment = `${company} is the best!`;
  const statment = `${myName} is testing ${company}\n${compliment} !`;

  console.log(statment);
}

printMyName("whydeeheem");